@model List<MVCDynamic7amBatch.Models.EmployeeModel>
@{
    ViewBag.Title = "getMeListEmployees";
}

<h2>getMeListEmployees</h2>

<table class="table table-bordered">
    <tr>
        <th>Id</th>
        <th>Name</th>
        <th>Salary</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.EmpId</td>
            <td>@item.EmpName</td>
            <td>@item.EmpSalary</td>
        </tr>
    }
</table>